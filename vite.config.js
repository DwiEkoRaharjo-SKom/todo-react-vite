import { defineConfig } from 'vite';

// Konfigurasi base path untuk GitHub Pages
export default defineConfig({
  base: '/todo-react-vite/', // Ganti dengan nama repositori Anda
  plugins: [
    // Masukkan plugin Anda di sini, jika ada
  ],
});